# 多物品比赛使用示例

> 假设现场问完工程师后的情况，演示完整操作流程。

---

## 场景设定

问完工程师后得知：
- 饮料A：**红色**可口可乐罐
- 饮料B：**黄色**冰红茶瓶
- 饮料C：**绿色**雪碧罐
- 衣物：红、黄、蓝三件上衣
- 所有饮料都投到**同一个T3点位**的黄色竹篓
- 所有衣物都投到**同一个T5点位**的黄色竹篓

---

## 第1步：改 items.conf

```bash
ssh ubuntu@192.168.10.2
cd ~/Documents/AutolifeXAIR
nano items.conf
```

改完后内容：

```
# === 饮料 ===
可乐红瓶|bottle_grasp_red.json|basket_drop.json|T2|T3|--reset-confirmed
冰茶黄瓶|bottle_grasp_yellow.json|basket_drop.json|T2|T3|--reset-confirmed
雪碧绿瓶|bottle_grasp_green.json|basket_drop.json|T2|T3|--reset-confirmed

# === 衣物 ===
红衣服|clothes_grasp_red.json|clothes_drop.json|T4|T5|--reset-confirmed
黄衣服|clothes_grasp_yellow.json|clothes_drop.json|T4|T5|--reset-confirmed
蓝衣服|clothes_grasp_blue.json|clothes_drop.json|T4|T5|--reset-confirmed
```

**items.conf 格式说明：**

```
名称|抓取JSON|投放JSON|抓取点|投放点|抓取额外flag
```

- 第1列：物品名称（随便写，只用于日志显示）
- 第2列：抓取用的JSON配置文件
- 第3列：投放用的JSON配置文件
- 第4列：导航到哪个点抓取（T2=饮料桌，T4=衣物桌）
- 第5列：导航到哪个点投放（T3=瓶篓，T5=衣篓）
- 第6列：抓取脚本的额外参数（`--reset-confirmed`表示确认已复位）

---

## 第2步：截帧确认HSV

每个物品都要截帧确认HSV能命中。

### 截帧命令（每个物品执行一次）

```bash
PY=/home/ubuntu/miniconda3/envs/robot_env/bin/python
mkdir -p evidence/calibration

# 把物品放到相机前，然后执行：
$PY - "evidence/calibration" <<'EOF'
import sys; sys.path.insert(0, "examples/camera")
import read_hand_camera as rhc, cv2
side = "left"
gv, list_outputs, open_consumer = rhc._load_sdk()
settings = rhc._resolve_settings(rhc._parser().parse_args(["--side", side]), gv)
outputs = rhc._catalog_outputs(list_outputs, settings, rhc.SIDE_MODULES[side])
output = rhc._select_output(outputs, settings["output_name"], side)
consumer = rhc._open_consumer(open_consumer, output, side)
try:
    image, frame_id, _ = rhc._poll_frame(consumer, side, settings["timeout"], settings["poll_interval"])
    cv2.imwrite(f"{sys.argv[1]}/hand_{side}.png", image)
    print(f"saved: {sys.argv[1]}/hand_{side}.png")
finally:
    rhc._close_consumer(side, consumer)
EOF
```

### 查看HSV值

用任意HSV查看工具（如GIMP、在线HSV picker）打开 `evidence/calibration/hand_left.png`，取瓶身颜色的HSV范围。

### 修改对应JSON

假设红瓶实际HSV是 `[0, 120, 100]` ~ `[8, 255, 255]`：

```bash
nano bottle_grasp_red.json
```

修改以下字段：

```json
"hsv_lower": [0, 120, 100],
"hsv_upper": [8, 255, 255],
"target_id": "coca-cola-red-can",
```

**需要修改的字段说明：**

| 字段 | 改什么 |
|---|---|
| `hsv_lower` | 截帧看到的HSV下限 |
| `hsv_upper` | 截帧看到的HSV上限 |
| `target_id` | 物品描述（只写进证据，随便改） |

**不用改的字段：** `camera_id`、`workcell_id`、`schema_version`（改了会报错）

### 同理处理其他物品

| 物品 | 截帧后改哪个JSON | 重点看什么 |
|---|---|---|
| 红瓶可乐 | `bottle_grasp_red.json` | HSV下限H≈0，上限H≈10 |
| 黄瓶冰茶 | `bottle_grasp_yellow.json` | HSV下限H≈15，上限H≈32 |
| 绿瓶雪碧 | `bottle_grasp_green.json` | HSV下限H≈35，上限H≈85 |
| 红衣服 | `clothes_grasp_red.json` | HSV下限H≈0，上限H≈10 |
| 黄衣服 | `clothes_grasp_yellow.json` | HSV下限H≈15，上限H≈32 |
| 蓝衣服 | `clothes_grasp_blue.json` | HSV下限H≈100，上限H≈130 |

**注意：** 衣物用的是**头部相机**，不是爪子相机。截帧命令把 `side = "left"` 改成用头部相机脚本。

---

## 第3步：预览

```bash
./flow_multi.sh
```

预期输出：

```
flow: 6 items loaded from items.conf

=== 预览模式（零动作）===

1. 验证导航点位...
   point T2: ok
   point T3: ok
   point T4: ok
   point T5: ok

2. 验证配置文件...
   可乐红瓶: ok (grasp=bottle_grasp_red.json, drop=basket_drop.json)
   冰茶黄瓶: ok (grasp=bottle_grasp_yellow.json, drop=basket_drop.json)
   雪碧绿瓶: ok (grasp=bottle_grasp_green.json, drop=basket_drop.json)
   红衣服: ok (grasp=clothes_grasp_red.json, drop=clothes_drop.json)
   黄衣服: ok (grasp=clothes_grasp_yellow.json, drop=clothes_drop.json)
   蓝衣服: ok (grasp=clothes_grasp_blue.json, drop=clothes_drop.json)

3. 执行顺序...
   reset@T1
   -> nav T2 -> 可乐红瓶 grasp -> reset -> nav T3 -> 可乐红瓶 drop -> reset
   -> nav T2 -> 冰茶黄瓶 grasp -> reset -> nav T3 -> 冰茶黄瓶 drop -> reset
   -> nav T2 -> 雪碧绿瓶 grasp -> reset -> nav T3 -> 雪碧绿瓶 drop -> reset
   -> nav T4 -> 红衣服 grasp -> reset -> nav T5 -> 红衣服 drop -> reset
   -> nav T4 -> 黄衣服 grasp -> reset -> nav T5 -> 黄衣服 drop -> reset
   -> nav T4 -> 蓝衣服 grasp -> reset -> nav T5 -> 蓝衣服 drop -> reset
   -> nav T1 (返回)

预览完成。确认无误后运行: ./flow_multi.sh go
```

**预览失败的常见原因：**

| 失败项 | 原因 | 修复 |
|---|---|---|
| `point T2: FAILED` | 地图不对 | 切比赛图+重新定位 |
| `MISSING bottle_grasp_red.json` | 文件名写错 | 检查items.conf第2列 |
| `MISSING examples/arm/run_bottle_grasp.py` | 脚本不存在 | 检查部署是否完整 |

---

## 第4步：实跑

```bash
./flow_multi.sh go
```

会先问确认：

```
=== 实跑模式 ===
确认：清场、有人监护、急停在手？(输入 go 继续)
go
```

**输入 `go` 后开始执行，全程不要离开。**

### 执行过程示例

```
[flow] Starting multi-item chain: 6 items

[flow] ==== reset@T1 ====
[flow] reset pose verified (x=0.15 z=0.98)
[flow] reset@T1 ok (19 s)

[flow] === Item 1/6: 可乐红瓶 ===
[flow] ==== nav_T2_可乐红瓶 ====
[flow] nav_T2_可乐红瓶 ok (14 s)

[flow] ==== grasp_可乐红瓶 ====
[flow] grasp_可乐红瓶 ok (27 s)

[flow] ==== reset_hold_可乐红瓶 ====
[flow] reset_hold_可乐红瓶 ok (18 s)

[flow] ==== nav_T3_可乐红瓶 ====
[flow] nav_T3_可乐红瓶 ok (10 s)

[flow] ==== drop_可乐红瓶 ====
[flow] drop_可乐红瓶 ok (20 s)

[flow] ==== reset_after_可乐红瓶 ====
[flow] reset_after_可乐红瓶 ok (18 s)

... （后续5个物品同理） ...

[flow] ==== nav_T1 ====
[flow] nav_T1 ok (15 s)

[flow] CHAIN COMPLETE. 6 items processed.
```

### 耗时估算

| 段 | 单次耗时 | 6个物品总计 |
|---|---|---|
| reset | ~18s | 6×18=108s |
| nav | ~12s | 6×12=72s（抓）+ 6×12=72s（投） |
| grasp | ~27s | 6×27=162s |
| drop | ~22s | 6×22=132s |
| **总计** | | **约476s（~8分钟）** |

加上初始复位和返回T1，全链约 **9-10分钟**。60分钟内绰绰有余。

---

## 如果某段失败

### 情况1：HSV没命中（no qualifying candidate）

```bash
# 看证据
cat evidence/grasp_冰茶黄瓶/<最新时间戳>/run_record.json | grep reason
# 输出：no qualifying candidate

# 原因：HSV没命中黄瓶
# 修复：截帧重标HSV
nano bottle_grasp_yellow.json
# 修改 hsv_lower/hsv_upper

# 单独重跑这一段
PY=/home/ubuntu/miniconda3/envs/robot_env/bin/python
$PY examples/arm/run_bottle_grasp.py run --execute --reset-confirmed --config bottle_grasp_yellow.json
```

### 情况2：验证带拒执行（grip verification failed）

```bash
# 看证据
cat evidence/grasp_可乐红瓶/<最新时间戳>/run_record.json | grep gripper_position
# 输出："gripper_position": 205

# 原因：grip_feedback_center不准
# 修复：把实际值回填到JSON
nano bottle_grasp_red.json
# 修改 "grip_feedback_center": 205

# 单独重跑
$PY examples/arm/run_bottle_grasp.py run --execute --reset-confirmed --config bottle_grasp_red.json
```

### 情况3：导航到不了点

```bash
# 看报错
cat evidence/nav_T3_冰茶黄瓶/<最新时间戳>/run_record.json | grep reason
# 输出：EXIT_MAP_ERROR

# 原因：地图不对
# 修复：切比赛图+重新定位
./examples/navigation/nav-test.sh locate
# 选择正确的点位，重新定位
```

### 铁律

**同一症状连续失败两次→停下，不第三次盲跑。考虑提前演示。**

---

## 如果投放点不同

假设红瓶投T3，黄瓶投T6，绿瓶投T7：

```
可乐红瓶|bottle_grasp_red.json|basket_drop.json|T2|T3|--reset-confirmed
冰茶黄瓶|bottle_grasp_yellow.json|basket_drop.json|T2|T6|--reset-confirmed
雪碧绿瓶|bottle_grasp_green.json|basket_drop.json|T2|T7|--reset-confirmed
红衣服|clothes_grasp_red.json|clothes_drop.json|T4|T5|--reset-confirmed
黄衣服|clothes_grasp_yellow.json|clothes_drop.json|T4|T5|--reset-confirmed
蓝衣服|clothes_grasp_blue.json|clothes_drop.json|T4|T5|--reset-confirmed
```

只需要改 `items.conf` 的第5列（投放点），JSON不用动。

**但要确保T6、T7点位在地图上存在。** 预览时会自动验证。

---

## 快速参考卡（比赛现场打印）

```
┌─────────────────────────────────────────────────────────┐
│  多物品比赛快速参考                                      │
├─────────────────────────────────────────────────────────┤
│  预览：  ./flow_multi.sh                                │
│  实跑：  ./flow_multi.sh go                             │
│  配置：  nano items.conf                                │
│  截帧：  $PY examples/camera/read_hand_camera.py --side left │
│  单跑：  $PY examples/arm/run_bottle_grasp.py run --execute --reset-confirmed --config bottle_grasp_red.json │
│  看证据：cat evidence/<段名>/<时间戳>/run_record.json     │
│  复位：  ./scripts/run_reset_robot.sh --execute --safety-acknowledged │
│  急停：  按物理急停按钮                                   │
└─────────────────────────────────────────────────────────┘
```
