# 4个标定JSON详解（调参速查手册）

> 所有JSON在 `E:\简历材料\比赛\AutolifeXAIR\` 根目录下。
> 代码守门模块在 `examples/arm/*_config.py`，改JSON前先确认值在安全限幅内。

---

## 总览

| 文件 | 环节 | 相机 | HSV目标 | 持物验证带 |
|---|---|---|---|---|
| `bottle_grasp.json` | T2抓瓶 | 手腕 | 瓶色(黄) | 188±5 |
| `basket_drop.json` | T3投瓶 | 头部RGBD | 篮色(黄) | 189±8 |
| `clothes_grasp.json` | T4抓衣 | 头部RGBD | 衣色(蓝) | 330±20 |
| `clothes_drop.json` | T5投衣 | 头部RGBD | 篮色(黄) | 330±20 |

**核心原则：换瓶/换衣→改HSV；换机/换桌→重标高度；验证带拒执行→读实际反馈值回填。**

---

## 1. bottle_grasp.json（T2 抓瓶）

**做什么：** 手腕相机找瓶子 → 视觉伺服逼近 → 夹住 → 验证 → 抬起。

### 身份字段（不能改）

| 字段 | 当前值 | 说明 |
|---|---|---|
| `schema_version` | 1 | 写死，改了报ConfigError |
| `camera_id` | `mod_camera_hand_left` | 代码严格比对，不一致拒跑 |
| `workcell_id` | `s2-test-point2-black-cloth` | 同上 |

### 身份字段（随便改，只写进证据）

| 字段 | 当前值 | 说明 |
|---|---|---|
| `robot_id` | `autolife-robot-274` | 标定来源记录 |
| `target_id` | `unified-yellow-tea-500ml` | 写成实际抓取物描述 |

### HSV检测（找瓶子）

| 字段 | 当前值 | 安全限幅 | 改法 |
|---|---|---|---|
| `hsv_lower` | [15, 80, 80] | H∈[0,179] S/V∈[0,255] | 截帧看HSV值；红色跨零写法 `[170,80,50]`/`[10,255,255]` |
| `hsv_upper` | [32, 255, 255] | S/V下限不能超上限 | S/V下限降低=容忍更暗光照，但会引入噪声 |
| `min_area` | 2000 | [1, 307200] | 太大→远处检测不到；太小→噪声误检 |
| `detection_roi_px` | [0, 100, 640, 380] | [x,y,w,h] 像素 | 瓶子总在画面偏下，y起始100；背景干扰多→收窄ROI |

### 对准参数（视觉伺服核心）

| 字段 | 当前值 | 含义 |
|---|---|---|
| `target_column_px` | 320.0 | 目标对准画面中央（640宽的一半） |
| `column_tolerance_px` | 20.0 | 列偏差≤20px视为已对中（≈0.5cm） |
| `column_to_y_sign` | -1 | 瓶子在画面右→机器人左移（物理实测确认） |
| `vertical_window_px` | [100.0, 460.0] | 瓶子中心行跑出此范围→中止 |

**视觉伺服逻辑：** 每轮"拍一帧→对准→前进"。先对准再前进，绝不边对中边前进。远处粗步(0.05m)，近了细步(0.005m)。宽度比≥0.39闭爪，>0.5冲过头中止。

### 运动参数

| 字段 | 当前值 | 安全限幅 | 改法 |
|---|---|---|---|
| `grasp_z_m` | 0.9 | 不得低于观察锚点下方0.25m | **最常改**。瓶子放桌上时=瓶心Z坐标。实测：首帧行150~180像素=高度合适 |
| `y_step_m` | 0.005 | ≤0.005m | 一般不动 |
| `x_coarse_step_m` | 0.05 | ≤0.05m | 远处粗步 |
| `x_fine_step_m` | 0.005 | ≤0.005m | 近处细步 |
| `fine_zone_width_ratio` | 0.3 | 必须≤close_width_ratio_min | 宽度比≥0.3切细步 |
| `close_width_ratio_min` | 0.39 | [0.01, 1.0] | **闭爪时机**。瓶子大小变了要重标 |
| `close_width_ratio_max` | 0.5 | 必须>close_width_ratio_min | 超过=冲过头中止 |
| `final_forward_m` | 0.12 | ≤0.15m | 闭爪前最后盲推 |
| `x_absolute_min_m` | 0.04 | [0.04, 0.70] | 防撞身体 |
| `x_absolute_max_m` | 0.7 | [0.04, 0.70] | 防伸太远 |

### 夹爪参数

| 字段 | 当前值 | 说明 | 改法 |
|---|---|---|---|
| `close_position` | 210.0 | [10, 360] 空夹闭合角度 | 一般不动 |
| `grip_feedback_center` | 188 | 堵转反馈中心 | **换瓶必标**。夹住瓶子后读反馈值，连续4次取中 |
| `close_tolerance` | 5.0 | [0.1, 5.0] 验证带半宽 | 188±5=[183,193]。放大→容错高但可能误判空夹为抓住 |
| `overall_timeout_s` | 180.0 | [1.0, 600.0] 总超时 | 一般不动 |

**安全设计：** `grip_feedback_center + close_tolerance < close_position`（188+5=193<210），空夹必然验证失败。

### 最常改的3个参数

| 场景 | 改哪个 | 怎么改 |
|---|---|---|
| 换瓶子颜色 | `hsv_lower/upper` | 爪子相机截帧看HSV值 |
| 瓶子位置/高度变了 | `grasp_z_m` | 实测瓶心Z坐标，首帧行150~180 |
| 换瓶子（大小/材质） | `grip_feedback_center` + `close_width_ratio_min` | 夹住后读堵转值4次取中 |

---

## 2. basket_drop.json（T3 投瓶）

**做什么：** 头部相机低头找黄色篮子 → 深度反投影定位 → 手臂移到篮上方 → 松爪释放 → 回撤。

### 流程

```
PREFLIGHT → VERIFY_HOLDING(确认还夹着瓶) → LOCATE(低头找篮) 
→ CONFIRM(二次定位) → MOVE_TO_HOVER(移到篮上方) → RELEASE(松爪) 
→ RETRACT(回撤) → COMPLETE
```

### 身份字段

| 字段 | 当前值 | 改法 |
|---|---|---|
| `camera_id` | `mod_camera_rgbd_head` | **不能改**（头部RGBD） |
| `workcell_id` | `s2-test-point3-baskets` | **不能改** |
| `target_id` | `yellow-basket` | 随便改（只记录） |

### HSV检测（找黄色篮子）

| 字段 | 当前值 | 说明 |
|---|---|---|
| `hsv_lower/upper` | 黄 [15,120,50]→[32,255,255] | S下限=120，比抓瓶更严（黄色篮子很黄） |
| `min_area` | 8000 | 比抓瓶2000大得多（篮子大、远，防误检） |
| `detection_roi_px` | [200, 60, 360, 340] | 只看画面中部，排除两侧 |

### 头部与深度

| 字段 | 当前值 | 改法 |
|---|---|---|
| `head_pitch_deg` | -35.0 | 低头35°。篮子矮/远→更负(如-40)；偏高→更接近0 |
| `rim_inset_px` | 12 | 筐沿向内收缩像素。筐沿识别不准→微调 |
| `depth_window` | 5 | [1,9] 奇数。深度不稳定→加大(如7) |
| `min_depth_m` | 0.45 | 深度下界 |
| `max_depth_m` | 1.6 | 深度上界。篮子实际距离变了→改窗口 |

### 运动参数（**最常改的一组**）

| 字段 | 当前值 | 说明 | 改法 |
|---|---|---|---|
| `hover_offset_m` | [-0.32, 0.022, 0.153] | 悬停点相对篮沿偏移[dx,dy,dz] | **瓶子擦篮壁→dx更负(如-0.37)；瓶子落不进→dx接近0** |
| `confirm_tolerance_m` | 0.05 | 两次定位偏差≤5cm才可信 | 一般不动 |
| `retract_position_m` | [0.15, 0.2, 1.0] | 松爪后回撤位置(绝对坐标) | 一般不动 |
| `envelope_x_m` | [0.0, 0.7] | 安全包络X | 篮子换位置→重标 |
| `envelope_y_m` | [-0.35, 0.45] | 安全包络Y | 同上 |
| `envelope_z_m` | [0.3, 1.4] | 安全包络Z | hover高度不能超1.4 |
| `y_align_x_m` | 0.6 | 横移限制线：Y横移只能在X≥0.6m | **篮筐后壁高前壁低，手必须在远侧横移** |
| `step_max_m` | 0.08 | [0.005, 0.10] 单步上限 | 想快→调大；想稳→调小 |

### 夹爪（持瓶验证）

| 字段 | 当前值 | 说明 |
|---|---|---|
| `held_feedback_center` | 189.0 | 持瓶堵转反馈中心（导航到T3后实测） |
| `held_feedback_tolerance` | 8.0 | 验证带半宽（189±8=[181,197]） |
| `open_position` | 10.0 | 松爪张开位置 |
| `gripper_tolerance` | 3.0 | 到位容差 |

**安全设计：** `|189-10|=179 > 8+3=11`，持物带和松开带永不重叠。

### 最常改的3个参数

| 场景 | 改哪个 | 怎么改 |
|---|---|---|
| 瓶子投不准/擦篮壁 | `hover_offset_m[0]`(dx) | dx更负=更靠后；更接近0=更靠前 |
| 低头看篮角度不对 | `head_pitch_deg` | 篮子矮/远→更负；偏高→更接近0 |
| 换了瓶子 | `held_feedback_center` | 实测持瓶反馈值回填 |

---

## 3. clothes_grasp.json（T4 抓衣）

**做什么：** 头部相机找蓝色衣物 → 腕部转向捏取姿态 → 水平对准 → 垂直下探 → 捏取 → 验证 → 抬起。

### 流程

```
PREFLIGHT → LOCATE(头部RGBD找蓝色衣物) → POSTURE(腕部转向捏取姿态) 
→ ALIGN(水平移到衣物上方) → DESCEND(垂直下探到衣物) 
→ PINCH(捏取+验证) → [空捏则微移重试一次] → LIFT_HOLD → COMPLETE
```

### 和抓瓶的本质区别

| | 抓瓶 | 抓衣 |
|---|---|---|
| 相机 | 手腕 | **头部** |
| 检测目标 | 刚性瓶子 | **软布料(可变形)** |
| 安全核心 | 不撞瓶/不扎桌 | **不扫桌沿/不扎穿桌面** |
| 姿态 | 无特殊 | **需要捏取四元数** |
| 重试 | 无 | **空捏后微移重试一次** |

### 身份字段

| 字段 | 当前值 | 改法 |
|---|---|---|
| `camera_id` | `mod_camera_rgbd_head` | **不能改**（头部RGBD） |
| `workcell_id` | `s2-test-point4-clothes-table` | **不能改** |

### HSV检测（找蓝色衣物）

| 字段 | 当前值 | 改法 |
|---|---|---|
| `hsv_lower/upper` | 蓝 [100,80,40]→[130,255,255] | 换衣颜色→改HSV。红衣 `[0,80,50]`/`[10,255,255]`+`[170,80,50]`/`[179,255,255]` |
| `min_area` | 3000 | 衣物折叠→适当调小(如2000) |
| `detection_roi_px` | [100, 100, 440, 240] | 衣物在画面偏上/下→调y起始 |

### 深度采样

| 字段 | 当前值 | 改法 |
|---|---|---|
| `erode_kernel_px` | 5 | [1,31] 奇数。太大会腐蚀没掩膜；太小引入噪点 |
| `min_valid_depth_px` | 200 | 掩膜内至少200个有效深度像素 |
| `min_depth_m` | 0.45 | 深度下界 |
| `max_depth_m` | 1.6 | 深度上界 |

### 头部角度

| 字段 | 当前值 | 改法 |
|---|---|---|
| `head_pitch_deg` | -35.0 | 和投瓶同理 |

### 桌面安全参数（**抓衣特有，核心安全逻辑**）

| 字段 | 当前值 | 说明 | 改法 |
|---|---|---|---|
| `table_height_m` | 0.75 | 桌面高度(m)，**现场卷尺实测** | **量错=扎桌或捏空！** 不同工位桌高不同 |
| `surface_consistency_window_m` | 0.1 | 感知衣物高度必须在桌面±0.1m内 | 一般不动 |
| `fingertip_offset_z_m` | 0.235 | 指尖到EEF的垂直偏移(m) | 260左臂值；**换机必须重标** |
| `pinch_rotation_quat` | [0.626, 0.324, -0.322, 0.632] | 捏取姿态四元数 | **不能直接镜像右臂**（260上ALIGN不收敛），以实机成功者为准 |

**推导高度（代码自动算）：**
- 捏取高度 = 0.75 + 0.01 + 0.235 = **0.995m**
- 安全高度 = 0.75 + 0.15 + 0.235 = **1.135m**
- 硬Z下限 = 0.75 - 0.02 + 0.235 = **0.965m**（再低=扎穿桌面）

### 运动偏移

| 字段 | 当前值 | 说明 |
|---|---|---|
| `grasp_offset_m` | [-0.027, 0.047, -0.02] | 感知→真值系统偏差修正。Z=-0.02=感知比实际高2cm |
| `retry_offset_m` | [0.02, 0.0] | 空捏重试：X+2cm微移，唯一一次机会 |
| `envelope_x_m` | [0.0, 0.78] | X上限0.78（衣物目标x=0.733） |
| `envelope_y_m` | [-0.35, 0.45] | 安全包络Y |
| `envelope_z_m` | [0.55, 1.3] | 安全包络Z（捏取高度0.995必须在此范围内） |
| `step_max_m` | 0.08 | 单步上限 |
| `descend_step_m` | 0.03 | 下探单步3cm（比step_max小，接近桌面要更慢） |

### 夹爪（捏布验证）

| 字段 | 当前值 | 说明 |
|---|---|---|
| `open_position` | 10.0 | 张爪位置 |
| `close_position` | 360.0 | 全力闭合（布料软，要捏到底） |
| `cloth_feedback_center` | 330.0 | 捏住衣物堵转中心（左爪实测320.7） |
| `cloth_feedback_tolerance` | 20.0 | 验证带半宽（330±20=[310,350]） |
| `gripper_tolerance` | 3.0 | 到位容差 |

**安全设计：** `|330-360|=30 > 20`（空捏=360在带外，捏住≈330在带内）。`|330-10|=320 > 20+3=23`（张开和捏住永不重叠）。

### 最常改的3个参数

| 场景 | 改哪个 | 怎么改 |
|---|---|---|
| 衣物颜色变了 | `hsv_lower/upper` | 相机截帧看HSV值 |
| 桌高不同 | `table_height_m` | **现场卷尺量！** |
| 空捏失败 | `cloth_feedback_center` + 堆厚衣物 | 实测堵转值回填 |

---

## 4. clothes_drop.json（T5 投衣）

**做什么：** 头部相机找T5篮子 → 移到篮上方 → 松爪释放衣物 → 回撤。**结构和投瓶完全复用**，只有标定数值不同。

### 和投瓶的差异

| | 投瓶 (basket_drop) | 投衣 (clothes_drop) | 为什么 |
|---|---|---|---|
| `held_feedback_center` | 189 | **330** | 衣物堵转值完全不同 |
| `held_feedback_tolerance` | 8 | **20** | 衣物带更宽 |
| `hover_offset_m[0]` (dx) | -0.32 | **-0.35** | 衣物更长，更靠后 |
| `hover_offset_m[2]` (dz) | 0.153 | **0.28** | **衣物垂下来很长，释放点必须更高** |
| `depth_window` | 5 | **9** | 衣物篮可能更深/反光更严重 |

### 关键字段

```json
"hover_offset_m": [-0.35, 0.023, 0.28]
```

**`hover_offset_m[2]=0.28`是最关键的值：** 衣物挂在爪子下方会垂下来很长，释放点必须更高才能越过篮壁。**但不能太大：** 0.28+感知高度如果>1.4m会撞envelope_z_m上限。

```json
"held_feedback_center": 330,
"held_feedback_tolerance": 20
```

和抓衣的 `cloth_feedback_center` 一致。投衣段VERIFY_HOLDING拒执行→读实际持衣反馈值回填。

### 最常改的3个参数

| 场景 | 改哪个 | 怎么改 |
|---|---|---|
| 衣物投不进/挂篮壁 | `hover_offset_m[2]`(dz) | dz更大=释放更高（注意上限1.4m） |
| 衣物落点偏 | `hover_offset_m[0]`(dx) + `hover_offset_m[1]`(dy) | 调偏移方向 |
| 换了衣物 | `held_feedback_center` | 实测持衣反馈值回填 |

---

## 通用调参原则

### 换瓶/换衣流程

1. 爪子相机截帧（`read_hand_camera.py`）确认HSV能命中色块
2. 实跑一次，读 `evidence/<段>/<时间戳>/run_record.json`
3. 如果验证带拒执行→读实际反馈值回填对应 `*_feedback_center`
4. 不要第三次盲跑

### 安全修改清单

| 改什么 | 影响 | 注意 |
|---|---|---|
| `table_height_m` | 抓衣全部推导高度 | **现场卷尺量！** |
| `fingertip_offset_z_m` | 捏取/安全/硬Z下限 | 换机必须重标 |
| `grip_feedback_center` | 抓瓶验证带 | 换瓶必标 |
| `cloth_feedback_center` | 抓衣/投衣验证带 | 换衣物材质必标 |
| `hover_offset_m` | 投放位置 | 每个工位单独标 |

### 不要随便改的

| 字段 | 为什么 |
|---|---|
| `camera_id` / `workcell_id` | 代码严格比对，不一致直接拒跑 |
| `pinch_rotation_quat` | 捏取姿态，改错=ALIGN不收敛 |
| `x_absolute_min/max_m` | 安全限幅，改了可能撞身体/伸太远 |
| `close_position`(抓瓶) | 空夹验证依赖这个值在带外 |
