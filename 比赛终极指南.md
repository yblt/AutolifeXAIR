# 比赛终极指南 v2（合并版）

> **本文档合并了官方《服务机器人应用赛技术指导手册》+ 仓库 README + 调参手册 + 上机清单。**
> **以本文档为准。** 编制日期 2026-08-28。

---

## 0. 官方赛事基本信息

| 项目 | 内容 |
|---|---|
| 比赛日期 | 2026年9月5日 |
| 比赛时长 | **1小时**（含部署、调测、任务执行） |
| 比赛电脑 | 赛事方统一提供，**不可联网**，配3米网线 |
| 禁止事项 | **不可使用AI工具、不可联网下载/安装依赖、不可携带手机/平板入场** |
| 机器人 | AutoLife S2，IP `192.168.10.2`，用户 `ubuntu` |
| 正式任务 | **12件物品**：3种饮料(每种1件) + 3色衣物(红/黄/蓝各1件) |
| 赛前练习 | 比赛前2天，每队1-2小时 |
| 提前演示 | 时间不够可提前要求演示，按已完成模块累计得分（不是0分） |

### 官方任务流程（语音触发）

```
语音"开始任务" → 鞠躬 → 播报"准备就绪，开始任务"
→ 从出发点自主移动到物品识别区
→ 识别并抓取目标物品
→ 运输到指定投放区，投放到对应容器
→ 全部完成后播报"任务完成"
```

### 投放规则

| 物品类型 | 识别依据 | 投放规则 |
|---|---|---|
| 饮料 | 饮料品类 | 按品类投放到对应容器 |
| 衣服 | 红/黄/蓝颜色 | 按颜色投放到对应容器 |

### 道具规格（官方）

| 项目 | 规格 |
|---|---|
| 识别区桌子 | 长240cm × 宽50cm × **高75cm** |
| 投放区桌子 | 长160cm × 宽50cm × **高75cm** |
| 投放纸箱/竹篓 | 长30cm × 宽30cm × 高20cm |
| 桌布 | 建议纯黑（便于视觉检测） |
| 阻隔墙 | 离地高度 > 50cm |
| 饮料尺寸 | 圆柱体或方块体，直径约50-70mm |

---

## 1. 关键差异：官方手册 vs 我们仓库

> **⚠️ 必须了解的差异，比赛现场不要搞混。**

| 项目 | 官方技术指导手册 | 我们仓库(比赛用) |
|---|---|---|
| 部署目录 | `~/Documents/xr-examples/` | `~/Documents/AutolifeXAIR/` |
| 抓瓶配置 | `green_bottle_grasp.json` | `bottle_grasp.json` |
| 抓瓶脚本 | `run_green_bottle_grasp.py` | `run_bottle_grasp.py` |
| 主用手相机 | **右手** `mod_camera_hand_right` | **左手** `mod_camera_hand_left`（260验证） |
| 点位命名 | P1-P5 | T1-T5（同一批点，不同名字） |
| 导航命令 | `nav-test navigate P2` | `nav-test.sh navigate T2` |
| 流程入口 | `./flow` | `./flow.sh` |
| 任务范围 | 12件(3饮料+3色衣物) | 1瓶+1蓝衣（基线代码） |

**为什么有差异：** 官方手册描述的是面向所有参赛队的通用框架（右臂链、P命名、xr-examples目录）。我们仓库是**左臂链专用优化版**（260号机全链验收通过），文件名和目录已改为左臂适配。**比赛用我们仓库的命令和文件名。**

---

## 2. 比赛60分钟时间分配

```
[0-3]   领任务书 + 问工程师（哪台机、什么瓶子、什么衣物、桌高）
[3-8]   开机必查（相机/权限/换机核对/地图点位）
[8-20]  快速调参（HSV/桌高/偏移）
[20-40] 逐段验证（T2→T3→T4→T5）
[40-55] flow.sh go 全链
[55-60] 余量/提前演示
```

**铁律：换机>3分钟没搞定导航→跳过，先调参拿已得分。40分钟时T2还没跑通→考虑提前演示。**

---

## 3. 第0步：拿到任务书（0-3分钟）

### 必问工程师的10个问题

| # | 问题 | 为什么重要 | 影响哪个文件 |
|---|---|---|---|
| 1 | **哪台机？（260还是274）** | **左臂链只在260验证过**，274是右臂值 | 全部JSON |
| 2 | 瓶子什么颜色？ | 决定HSV要不要重标 | `bottle_grasp.json` |
| 3 | 瓶子尺寸/重量和仓库样品一样吗？ | 尺寸变了→`close_width_ratio_min`和`grip_feedback_center`都变 | `bottle_grasp.json` |
| 4 | 衣物什么颜色/材质？ | 颜色→改HSV；材质→改`cloth_feedback_center` | `clothes_grasp.json` |
| 5 | **现场桌高多少？**（请工程师用卷尺量） | **量错=扎桌或捏空！** | `clothes_grasp.json` |
| 6 | 篮子摆位和录点时一样吗？ | 摆位变了→`hover_offset_m`要重标 | `basket_drop.json` / `clothes_drop.json` |
| 7 | 比赛用哪张地图？T1-T5点位叫什么？ | 点位名不匹配→`EXIT_MAP_ERROR` | `flow.sh` |
| 8 | 比赛时间限制？提前演示机制怎么触发？ | 心中有数，合理分配60分钟 | 策略 |
| 9 | 有没有已经标定好的JSON可以直接用？ | 如果有→直接用，省大量时间 | 全部 |
| 10 | 机器人IP/密码？网线直连还是WiFi？ | rsync/ssh部署要用 | 部署流程 |

**同时做的事：**

```bash
# 连接机器人
ssh ubuntu@192.168.10.2
cd ~/Documents/AutolifeXAIR
```

---

## 4. 第1步：开机必查（3-8分钟）

### 4a. 基础检查

```bash
# 相机有没有
ls /dev/shm | grep -E "hand_left|rgbd_head"

# 脚本能不能跑
ls -l flow.sh scripts/*.sh
```

**没有相机→** `systemctl --user restart vision-service`，等40秒
**脚本没x位→** `chmod +x flow.sh setup.sh scripts/*.sh examples/navigation/nav-test.sh`
**有CRLF报错→** `sed -i 's/\r$//' flow.sh setup.sh scripts/*.sh examples/navigation/nav-test.sh`

### 4b. 换机/重刷镜像后必做（2分钟，只做一次）

如果这台机器被重刷过镜像或换过，以下3项**出厂全部还原**，必须重做：

**① 手腕相机模块有没有启用（最关键的1项）：**

```bash
find ~/miniconda3/envs/robot_env -path "*/configs/robot_v2_2.json" | xargs grep -l "mod_camera_hand_left" | head -1 | xargs grep "ENABLED_MODULES"
```

输出必须包含 `mod_camera_hand_left`。没有→**左臂抓瓶段一定失败**。找工程师处理。

**② 导航参数4项是否还原出厂（起步转圈的根因）：**

```bash
# 快速检查导航是否正常
cd ~/Documents/AutolifeXAIR
./examples/navigation/nav-test.sh preview T1
```

输出 `active_map` 不对 → 切图+重新定位后还是转圈 → 问工程师确认 `nav2_params.yaml` 四项：

- `local_costmap plugins` 必须含 `inflation_layer`
- `global_costmap plugins` 必须含 `inflation_layer`
- `FollowPath.min_obstacle_dist` = 0.20（出厂0.00会贴墙起步就卡死）
- `xy_goal_tolerance` = 0.05（出厂0.01太严会到点后蹭位不停）

**③ 雷达方向有没有反（导航冲向操作者）：**

```bash
# 导航到T1后，看机器人是否朝正确方向
./examples/navigation/nav-test.sh navigate T1 --execute --safety-acknowledged --feedback-timeout 60
```

观察：机器人前进方向是否和"外观车头"一致。反了→问工程师核对雷达TF配置。

### 4c. 官方10项只读自检（合并）

| # | 检查项 | 命令 | 通过标准 |
|---|---|---|---|
| 1 | 网络连通 | PC侧 `Test-NetConnection 192.168.10.2 -Port 22` | `TcpTestSucceeded : True` |
| 2 | 主机身份 | `hostname` | `autolife-robot-274`（或对应编号） |
| 3 | 关键服务 | `systemctl --user is-active vision-service arm-control-service gv-slam-service gv-control-service` | 每行 `active` |
| 4 | 导航接口 | `ros2 service type /robot_map_0_274/map_command` | `autolife_robot_srvs/srv/SetString` |
| 5 | 活动地图 | `nav-test preview T1` | 活动地图正确，点位校验通过 |
| 6 | 末端状态 | `ros2 topic echo --once /topic_arm_current_robot_eef_pose_0_274` | 返回左右末端位姿JSON |
| 7 | 夹爪状态 | `ros2 topic echo --once /topic_arm_whole_body_and_gripper_current_joints_status_0_274` | 返回左右夹爪状态 |
| 8 | 手相机 | `python examples/camera/read_hand_camera.py --side left` | `status: "ok"`，图像尺寸 `[480,640,3]` |
| 9 | 头部RGBD | `python examples/camera/read_head_rgbd.py` | 可读取彩色BGR与深度帧 |
| 10 | 急停 | 现场目视 | 急停未按下，监护人员可立即触达 |

### 4d. 地图和点位

```bash
./flow.sh
```

看输出：`point T1: ok` ~ `point T5: ok`
**全部ok→** 进下一步
**FAILED→** 菜单切比赛图 + 重新初始定位

### 4e. 官方点位参考（GZRBXR08地图）

| 点名 | 用途 | x, y, yaw° |
|---|---|---|
| P1/T1 | 出发/返回 | 0.765, -0.579, 29.0 |
| P2/T2 | 饮料桌，抓瓶 | 0.024, 1.138, -152.34 |
| P3/T3 | 瓶投放篓 | 0.333, 3.461, 119.34 |
| P4/T4 | 衣物桌，抓衣 | -0.595, 2.283, -145.15 |
| P5/T5 | 衣物投放容器区 | 1.404, 2.529, 28.14 |

> 具体点名以比赛现场地图为准。P1-P5和T1-T5是同一批点的不同命名。

---

## 5. 第2步：环境前置（每次手动跑Python前必做）

```bash
cd ~/Documents/AutolifeXAIR
export PYTHONPATH="/home/ubuntu/Documents/AutolifeRobotSDK/sdk_python:$PWD${PYTHONPATH:+:$PYTHONPATH}"
PY=/home/ubuntu/miniconda3/envs/robot_env/bin/python
```

> **注意：先激活 Conda，再 source ROS。顺序颠倒可能导致ros2命令能看到缓存列表但无法连接真实服务。**
> `flow.sh`、`nav-test.sh`、`arm_move.sh` 已自动加载环境，不需要手动执行本段。

---

## 6. 第3步：快速调参（8-20分钟）

### 6a. 瓶子颜色变了？只改这里：

```json
// bottle_grasp.json
"hsv_lower": [新H, 新S, 新V],
"hsv_upper": [新H, 新S, 新V]
```

**不知道新HSV？** 用爪子相机截帧看：

```bash
cd ~/Documents/AutolifeXAIR
mkdir -p evidence/calibration
/home/ubuntu/miniconda3/envs/robot_env/bin/python - "evidence/calibration" <<'EOF'
import sys; sys.path.insert(0, "examples/camera")
import read_hand_camera as rhc, cv2
side = "left"
gv, list_outputs, open_consumer = rhc._load_sdk()
settings = rhc._resolve_settings(rhc._parser().parse_args(["--side", side]), gv)
outputs = rhc._catalog_outputs(list_outputs, settings, rhc.SIDE_MODULES[side])
output = rhc._select_output(outputs, settings["output_name"], side)
consumer = rhc._open_consumer(open_consumer, output, side)
try:
    image, frame_id, _ = rhc._poll_frame(consumer, side, settings["timeout"], settings["poll_interval"])
    cv2.imwrite(f"{sys.argv[1]}/hand_{side}.png", image)
    print(f"saved: {sys.argv[1]}/hand_{side}.png  frame_id={frame_id}")
finally:
    rhc._close_consumer(side, consumer)
EOF
```

看截图里瓶子颜色的HSV值，填入JSON。

**HSV调参速查：**

| 问题 | 建议 |
|---|---|
| 红色检测不稳定 | 使用两段HSV红色范围，合并mask（`[170,80,50]`/`[10,255,255]`） |
| 黄/蓝误检背景 | 缩小ROI，只看桌面或夹爪前方区域 |
| 光照变化大 | 赛前练习阶段采集样本，按现场光照调HSV阈值 |
| 多个候选 | 增加面积、长宽比、位置、连续帧稳定性筛选 |
| 颜色相近 | 用"颜色+形状+位置"组合判断，不只靠单一阈值 |

### 6b. 桌高变了？只改这里：

```json
// clothes_grasp.json
"table_height_m": 现场量的桌高
```

**官方桌高标准：75cm (0.75m)。不同工位可能不同，必须现场卷尺量！**

### 6c. 投放不准？只改这里：

```json
// basket_drop.json 或 clothes_drop.json
"hover_offset_m": [dx, dy, dz]
// 瓶子擦篮壁→dx更负(如-0.37)
// 衣物挂篮壁→dz更大(如0.35)，但不能超1.4m
```

### 6d. 验证带拒执行→回填反馈值

| 失败类型 | 读哪里 | 回填到 |
|---|---|---|
| 抓瓶验证带拒执行 | `evidence/bottle_grasp/<时间戳>/run_record.json` 的 `gripper_position` | `bottle_grasp.json` → `grip_feedback_center` |
| 投瓶VERIFY_HOLDING拒执行 | `evidence/basket_drop/<时间戳>/run_record.json` | `basket_drop.json` → `held_feedback_center` |
| 抓衣验证带拒执行 | `evidence/clothes_grasp/<时间戳>/run_record.json` | `clothes_grasp.json` → `cloth_feedback_center` |
| 投衣VERIFY_HOLDING拒执行 | `evidence/clothes_drop/<时间戳>/run_record.json` | `clothes_drop.json` → `held_feedback_center` |

---

## 7. 第4步：逐段验证（20-40分钟）

**每段先preview再run：**

```bash
PY=/home/ubuntu/miniconda3/envs/robot_env/bin/python

# T2 抓瓶
$PY examples/arm/run_bottle_grasp.py preview --config bottle_grasp.json
$PY examples/arm/run_bottle_grasp.py run --execute --reset-confirmed --config bottle_grasp.json

# T3 投瓶
$PY examples/arm/run_basket_drop.py preview --config basket_drop.json
$PY examples/arm/run_basket_drop.py run --execute --config basket_drop.json

# T4 抓衣
$PY examples/arm/run_clothes_grasp.py preview --config clothes_grasp.json
$PY examples/arm/run_clothes_grasp.py run --execute --reset-confirmed --config clothes_grasp.json

# T5 投衣
$PY examples/arm/run_clothes_drop.py preview --config clothes_drop.json
$PY examples/arm/run_clothes_drop.py run --execute --config clothes_drop.json
```

**某段失败→先读证据：**

```bash
ls evidence/<段名>/
cat evidence/<段名>/<最新时间戳>/run_record.json | grep reason
```

**别重跑，先修。**

### 各段状态机流程

**抓瓶 (run_bottle_grasp.py):**
```
PREFLIGHT → SERVO(视觉伺服逼近) → CLOSE(闭爪) → VERIFY(验证) → LIFT(抬起) → COMPLETE
```

**投瓶 (run_basket_drop.py):**
```
PREFLIGHT → VERIFY_HOLDING(确认还夹着瓶) → HEAD_PITCH(低头) → LOCATE(找篮)
→ CONFIRM(二次定位) → MOVE_TO_HOVER(移到篮上方) → RELEASE(松爪) → RETRACT(回撤) → COMPLETE
```

**抓衣 (run_clothes_grasp.py):**
```
PREFLIGHT → LOCATE(头部RGBD找蓝色衣物) → POSTURE(腕部转向捏取姿态)
→ ALIGN(水平对准) → DESCEND(垂直下探) → PINCH(捏取+验证)
→ [空捏则微移重试一次] → LIFT_HOLD → COMPLETE
```

**投衣 (run_clothes_drop.py):**
```
PREFLIGHT → VERIFY_HOLDING(确认还夹着衣) → LOCATE(找篮)
→ CONFIRM(二次定位) → MOVE_TO_HOVER(移到篮上方) → RELEASE(松爪) → RETRACT(回撤) → COMPLETE
```

---

## 8. 第5步：全链（40-55分钟）

```bash
./flow.sh go
```

输入 `go`。

### 全链流程

```
reset@T1 → nav_T2 → bottle_grasp → reset_hold_bottle → nav_T3 → bottle_drop
→ reset_after_bottle → nav_T4 → clothes_grasp → reset_hold_cloth → nav_T5
→ clothes_drop → reset_final → nav_T1
```

### 官方验收耗时参考（2026-08-17赛场验收）

| 段 | 耗时s | 段 | 耗时s |
|---|---|---|---|
| reset@P1 | 19 | reset_after_bottle | 18 |
| nav_P2 | 14 | nav_P4 | 10 |
| bottle_grasp | 27 | clothes_grasp | 26 |
| reset_hold_bottle | 18 | reset_hold_cloth | 18 |
| nav_P3 | 10 | nav_P5 | 10 |
| bottle_drop | 20 | clothes_drop | 25 |
| reset_final | 19 | nav_P1 | 15 |
| **端到端总计** | **249s (约4分9秒)** | | |

---

## 9. 报错速查表

| 报错关键词 | 原因 | 10秒修复 |
|---|---|---|
| `Permission denied` | 没x位 | `chmod +x` |
| `$'\r': command not found` | CRLF | `sed -i 's/\r$//' *.sh` |
| `EXIT_MAP_ERROR` | 地图不对 | 切比赛图+重新定位 |
| `catalogued but shared memory` | 相机没注册 | `systemctl --user restart vision-service` |
| `posix_ipc is not installed` | 用错python | 用 `$PY`（robot_env） |
| `no qualifying candidate` | HSV没命中 | 截帧重标HSV |
| `width ratio overshot` | 冲过头 | 减小 `x_coarse_step_m` 或瓶子摆远点 |
| `candidate vertical center left the window` | 高度异常 | 调 `grasp_z_m` |
| `grip verification failed` | 抓空 | 读 `run_record.json` 回填 `grip_feedback_center` |
| `invalid depth` | 深度异常 | 移除反光杂物；加大 `depth_window` |
| `envelope rejected` | 超安全包络 | 调 `envelope_*_m` 或恢复篮子原位 |
| `Y correction would exceed limit` | Y偏太多 | 恢复瓶子到画面中央附近 |
| `overall timeout` | 太慢 | 加大 `overall_timeout_s` 或调步长 |

---

## 10. 关键命令速查

```bash
# ===== 导航 =====
./examples/navigation/nav-test.sh preview T1                    # 预览点位（零动作）
./examples/navigation/nav-test.sh navigate T2 --execute --safety-acknowledged --feedback-timeout 180  # 导航到点
./examples/navigation/nav-test.sh locate                        # 初始定位

# ===== 机械臂 =====
./scripts/arm_move.sh --arm left --direction forward --distance 0.10 --execute  # 前伸
./scripts/arm_move.sh --arm both --gripper open --execute        # 夹爪张开
./scripts/arm_move.sh --arm both --gripper close --execute       # 夹爪闭合

# ===== 整机 =====
./scripts/run_reset_robot.sh --execute --safety-acknowledged    # 整机复位
./scripts/head_pitch.sh --pitch -35 --execute --safety-acknowledged  # 低头

# ===== 相机 =====
./scripts/head_probe.sh capture --output-dir evidence/calibration --frames 1  # 头部相机截帧

# ===== 全链 =====
./flow.sh                         # 只读预览（零动作）
./flow.sh go                      # 实跑全链
```

---

## 11. 证据记录（跑完每段必看）

```
evidence/
├── bottle_grasp/<时间戳>/run_record.json + 帧jpg
├── basket_drop/<时间戳>/run_record.json + 帧jpg
├── clothes_grasp/<时间戳>/run_record.json + 帧jpg
├── clothes_drop/<时间戳>/run_record.json + 帧jpg
└── flow/<时间戳>/chain_log.csv
```

**每次失败后先读 `run_record.json` 的 `reason` 字段和证据帧**，不要凭感觉重跑。

---

## 12. 安全红线

- 实体动作必须带 `--execute`
- 紧急用**物理急停**（软件中止不可靠）
- 失败后**不自动重试、不自动松爪、不自动回退**
- 同一症状连续两次失败→**停下，不第三次盲跑**
- 禁止绕过 `--execute`、`--safety-acknowledged` 等安全确认门
- 禁止扩大机械臂运动限幅
- 禁止在人员未清场、急停不可触达时执行实体运动
- 禁止在目标识别不确定时强行抓取

---

## 13. 官方开发边界

### 允许事项
- 提前在自己电脑上开发和调试代码
- 运行赛事方提供的包装命令（nav-test、arm-move、flow）
- 用 `$PY examples/arm/...` 调用各模块运行器
- 修改和运行机器人上的示例脚本或自带脚本（遵守安全限幅）
- 直接调用 ROS topic/service，使用已安装的Python、ROS2、OpenCV、NumPy等

### 禁止事项
- 禁止使用AI工具
- 禁止联网下载、安装或更新依赖
- 禁止关闭、绕过或弱化急停、限幅、安全确认门
- 禁止扩大机械臂运动限幅、绕过夹爪限位
- 禁止直接修改厂商SDK、系统服务或底层控制配置（除非裁判授权）
- 禁止在人员未清场时执行实体运动

---

## 附录A：提前演示触发条件

以下任一情况→立刻要求演示：

- 40分钟时还没完成全链
- 某段连续两次同一症状失败
- 你判断剩余时间不够跑完

**演示按已完成模块累计得分，不是0分。**

---

## 附录B：部署命令备忘

```bash
# 1. 去CRLF（Windows编辑过必做）
sed -i 's/\r$//' flow.sh setup.sh scripts/*.sh examples/navigation/nav-test.sh

# 2. rsync上机
rsync -av --delete --exclude='.git' --exclude='evidence/' \
  ./ ubuntu@192.168.10.2:~/Documents/AutolifeXAIR/

# 3. 上机后补可执行位+预览自检
ssh ubuntu@192.168.10.2 'cd ~/Documents/AutolifeXAIR \
  && chmod +x flow.sh setup.sh scripts/*.sh examples/navigation/nav-test.sh \
  && ./flow.sh'
```

---

## 附录C：官方全流程操作清单

### 入场后检查
1. 确认比赛电脑、网线、U盘和队伍代码
2. 连接机器人网络
3. SSH登录机器人
4. 确认主机名
5. 复制或同步代码
6. 不安装新依赖
7. 执行只读自检
8. 确认急停、监护和场地清空

### 调试顺序

| 顺序 | 内容 | 通过标准 |
|---|---|---|
| 1 | 网络与SSH | 成功登录目标机器人 |
| 2 | 服务与环境 | 关键服务active，ROS接口可读 |
| 3 | 地图与定位 | 活动地图正确，定位稳定 |
| 4 | 空载导航 | 出发点、识别区、投放区可往返 |
| 5 | 相机读帧 | 右手相机返回有效图像 |
| 6 | 颜色检测 | 能稳定输出目标颜色和候选框 |
| 7 | 头部深度相机 | 彩色/深度帧可读，投放模块preview可输出计划 |
| 8 | 机械臂动作 | 小步动作和夹爪开合正常 |
| 9 | 单件闭环 | 单件物品抓取、投放、返回 |
| 10 | 多件任务 | 行为树/状态机循环执行 |

### 正式运行

正式运行前确认：
- 机器人在出发点
- 物品摆放符合规则
- 投放容器位置确认
- 程序已部署
- 急停人员就位
- 裁判确认可以开始

正式任务开始后机器人应：
1. 接收语音开始任务
2. 鞠躬示意
3. 播报"准备就绪，开始任务"
4. 执行抓取投放流程
5. 完成后播报"任务完成"

---

## 附录D：官方异常处理表

| 异常 | 处理 |
|---|---|
| 机器人路径异常 | 立即取消导航；危险时按急停；重新定位后从出发点开始 |
| 手臂动作过大 | 立即按急停；由工作人员确认安全后复位 |
| 夹爪无动作 | 不连续重发；检查状态反馈，必要时执行整机复位 |
| 相机无帧 | 检查服务状态和相机脚本；不要切换到未验证相机 |
| 投放模块拒绝执行 | 未持物/检测候选歧义/深度无效/停靠点偏移 → 保持夹持停止，先preview再检查 |
| 头部深度读数异常 | 保持uint16深度原始值，缩小ROI，重新采样和验证 |
| 投放错误 | 由裁判或工作人员取出并放回识别区 |
| 程序异常退出 | 保留日志，确认机器人静止后重新部署或重跑 |

---

## 附录E：官方行为树/状态机结构

```
INIT
  → WAIT_START_COMMAND
  → BOW_AND_ANNOUNCE
  → RESET_AT_P1
  → NAV_TO_P2
  → BOTTLE_GRASP
  → RESET_HOLD_BOTTLE
  → NAV_TO_P3
  → BOTTLE_DROP
  → RESET_AFTER_BOTTLE
  → NAV_TO_P4
  → CLOTHES_GRASP
  → RESET_HOLD_CLOTH
  → NAV_TO_P5
  → CLOTHES_DROP
  → RESET_FINAL
  → NAV_TO_P1
  → FINISH
```

每段至少记录：段名、开始和结束时间、退出码、关键反馈值、是否进入fail-closed、恢复建议。
